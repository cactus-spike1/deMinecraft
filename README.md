

## 📌 Что такое деобфускация?

Деобфускация — процесс восстановления исходных имён классов, методов и полей, которые были изменены обфускатором. Этот гайд поможет провести деобфускацию `.jar` файла с использованием маппингов.

---

## 🔹 1. Что понадобится?

- **SpecialSource** (для применения маппингов) — [💾 Скачать](https://ci.md-5.net/job/SpecialSource/)
- **CFR** (для декомпиляции) — [💾 Скачать](https://github.com/leibnitz27/cfr)
- **Recaf** (для анализа и редактирования байткода) — [💾 Скачать](https://github.com/Col-E/Recaf/releases)

---

## 🔹 2. Применение маппингов

Допустим, у нас есть:

- `client.jar` (обфусцированный файл)
- `client.txt` (маппинги)

Теперь размаппим `.jar` с помощью **SpecialSource**:

```sh
java -jar SpecialSource.jar map -i client.jar -o client_deobf.jar -m client.txt
```

✅ Теперь `client_deobf.jar` содержит настоящие имена классов и методов!

---

## 🔹 3. Декомпиляция JAR-файла

Теперь извлечём `.java` файлы из `.jar` с помощью **CFR**:

```sh
java -jar cfr.jar client_deobf.jar client/
```

После выполнения команды в папке `client/` появятся `.java` файлы с читаемым кодом.

---

## 🔹 4. Анализ и редактирование

Если тебе нужно проанализировать `.jar`, можешь открыть его в **Recaf**:

```sh
java -jar recaf.jar
```

Открой `client_deobf.jar`, и ты увидишь дерево классов с возможностью их редактирования.

---

## 🎯 Итог

1. **Применяем маппинги** с помощью `SpecialSource`.
2. **Декомпилируем код** с `CFR`.
3. **Анализируем и редактируем** в `Recaf`.

Теперь у тебя есть полностью читаемый код! 🔥 Если есть вопросы — спрашивай! 🚀
